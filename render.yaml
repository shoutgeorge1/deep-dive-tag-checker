services:
  - type: web
    name: deep-dive-tag-checker
    env: node
    buildCommand: npm install && npx playwright install chromium
    startCommand: node server.js
    plan: free
    healthCheckPath: /health
    envVars:
      - key: NODE_ENV
        value: production

